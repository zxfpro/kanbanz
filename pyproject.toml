[project]
name = "kanbanz"
version = "0.1.1"
description = "一个看板调度能力的SDK包"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mkdocs-mermaid2-plugin>=1.2.1",
    "pytest>=8.3.5",
    "pytest-html>=4.1.1",
    "setuptools>=78.1.0",
]
